# تحديث v3 — رد على كل النقط الـ 14

## التركيب
1. حمّل `update-v3.ps1` و `poyekhali-visa-v3-kit.zip` وحطهم مع بعض.
2. من جذر المشروع:
   ```powershell
   cd D:\work\Poyekhali
   powershell -ExecutionPolicy Bypass -File "المسار\update-v3.ps1" -ZipPath "المسار\poyekhali-visa-v3-kit.zip"
   ```
3. اقرأ آخر رسالة بيطبعها السكريبت (فيها خطوات .env يدوية لازم تعملها).

---

## رد على كل نقطة

**1) 6 لغات جديدة**: روسي، فرنسي، ألماني، إيطالي، أسباني، صيني مضافين بالكامل (136 مفتاح ترجمة في كل لغة، مطابقين تمامًا للعربي والإنجليزي). زرار اللغة بقى قائمة منسدلة بدل الزرار الواحد (مش هيبقى عملي إنه toggle مع 8 لغات).

**2) جدول مرفقات منفصل**: عملت بالظبط زي ما اقترحت — جدول `submission_attachments` مربوط بـ `contact_submissions`، فورم "تواصل معنا" بقى بياخد أكتر من ملف مرة واحدة.

**3) مشكلة تحميل المرفق**: السبب كان اعتماد الرابط على `storage:link` (اللي بيفشل كتير على ويندوز من غير صلاحيات Admin). استبدلته براوت مخصص بيخدّم الملف من Laravel نفسه (`Storage::disk('public')->download()`)، هيشتغل مهما كان وضع الـ symlink.

**4) عرض الصورة مباشرة**: صفحة عرض الرسالة في لوحة التحكم دلوقتي بتعرض أي مرفق صورة inline (`<img>`) من غير ما تحتاج تحمّلها، والملفات التانية (PDF مثلاً) بتظهر كرابط تحميل.

**5) اللوجو "Поехали!"**: الهيدر بقى فيه "Поехали!" بخط Unbounded (بيدعم السيريلية والعرض الجريء ده مناسب لموضوع الفضاء). حطيت **صورة حقيقية اتأكدت إنها شغالة** لصاروخ سويوز بيتنقل لمنصة الإطلاق (رخصة عامة CC0 من NASA، تصوير Carla Cioffi) كمكان مؤقت، جاهز تستبدلها بصورة يوري جاجارين بتاعتك أي وقت — غيّر متغير `logoImage` في `resources/js/Components/AppHeader.vue`.
   - **ملحوظة مهمة**: صور يوري جاجارين الحقيقية (خصوصًا من مصادر الأخبار/الأرشيف السوفيتي) غالبًا محمية بحقوق نشر لجهات زي TASS أو Roscosmos، فمينفعش أحطلك رابط مباشر ليها من غير ترخيص. لو عندك صورة مرخّصة ليك، ارفعها في `public/images/logo-astronaut.jpg` وغيّر `logoImage` لـ `/images/logo-astronaut.jpg`.

**6) خلفية متغيرة في الهيرو**: ضفت `hero-slides` بتتبدل تلقائيًا كل كذا ثانية (CSS keyframes، من غير JavaScript إضافي). حاليًا فيها 4 صور placeholder بنفس صورة الصاروخ اللي فوق (تكرار كما طلبت لحد ما تحط صورك). لتغييرها: حط صورك في `public/images/hero/1.jpg` لغاية `4.jpg` وعدّل مصفوفة `heroSlides` في `Home.vue`.

**7) تسجيل الدخول/عضوية جديدة**: لينكات `/login` و `/register` في الهيدر (منفصلة تمامًا عن `/admin/login` بتاع لوحة التحكم — ده حساب عميل عادي).

**8) تتبع حالة الطلب**: صفحة `/track` — بتاخد رقم الطلب ورقم جواز السفر، وترجع الحالة مترجمة حسب اللغة. الحالات المتاحة (مفتاح إنجليزي ثابت في قاعدة البيانات):
   - `under_review` تحت الدراسة
   - `approved_processing` تم قبول الطلب وجارٍ العمل على استخراج التأشيرة
   - `visa_ready` التأشيرة جاهزة
   - `visa_cancelled` تأشيرة ملغاة
   - `deleted` طلب محذوف
   - `other` حالة أخرى
   تقدر تغيّر حالة أي طلب من `/admin/applications`.

**9) السيدر التلقائي**: `database/seeders/DatabaseSeeder.php` بقى بينادي على `VisaCatalogSeeder` و `AdminUserSeeder` تلقائيًا، فـ:
   ```powershell
   php artisan migrate:fresh --seed
   ```
   هيزرع الدول العشرة (مصر، روسيا، العراق، الإمارات، الكويت، عُمان، قطر، السعودية، البحرين، ليبيا) وكل نوع تأشيرة بمستنداته اللي بعتّهالي بالظبط. **ملحوظة**: السكريبت `update-v3.ps1` نفسه بيشغّل `migrate` عادي مش `migrate:fresh` عشان متفقدش رسائل/طلبات موجودة بالفعل — الأمر `migrate:fresh --seed` سيبته ليك تشغّله يدويًا وقت ما تحب مسح كل حاجة والبدء من جديد.
   - **الرسوم ومدة الإجراءات** حطيت لها قيم مبدئية معقولة (مش مذكورة في اللي بعتّهولي) — عدّلها من `/admin/countries` حسب الأسعار الحقيقية بتاعتك.

**10 + 11) `is_active` وتعديل السكيمة**: نفذت السكيمة بالظبط زي ما حددتها:
   - `countries`: `slug, flag, name_ar, name_en, region, processing_time_en, image_path, order, is_active`
   - `visa_types`: `country_id, key, name_ar, name_en, processing_time_ar, fee, order, is_active`
   لما تعطّل دولة أو نوع تأشيرة من لوحة التحكم، بيختفي من الموقع فورًا.

**12 + 13) بيانات التواصل**: صفحة "تواصل معنا" بقى فيها إيميل المرسل (حقل في الفورم)، رابط تيليجرام `@hekal_85`، واتساب `+79936445881`. وأعلى الموقع اتحدّث لـ `hekal_85@hotmail.com` و `+79936445881`. كل البيانات دي جايه من `config/site.php` (قابلة للتعديل من `.env` من غير ما تلمس الكود).

**14) صفحة "ابدأ طلبك" (`/apply`)**: فيها بالظبط كل الحقول اللي طلبتها (اسم، رقم جواز سفر، دولة، نوع تأشيرة معتمد على الدولة المختارة، إيصال دفع اختياري، مرفقات متعددة بعمودين — نوع المستند + الملف، عنوان، هاتف، إيميل، ملاحظات). بعد الإرسال:
   - بيتولّد رقم طلب فريد (`PYK-000123`).
   - **إيميل تلقائي** للعميل (لو دخل إيميله) بصيغة رسمية فيها رقم الطلب.
   - **تيليجرام تلقائي**: بيوصل لبوت تيليجرام بتاع **الإدارة** (مش العميل) كتنبيه بطلب جديد — التفاصيل تحت في "قيود لازم تعرفها".
   - **واتساب تلقائي للعميل**: شغّال فعليًا لو ربطت حساب WhatsApp Business API (Twilio/UltraMsg/Wassenger)، التفاصيل تحت.

---

## قيود لازم تعرفها (بصراحة كاملة)

- **واتساب/تيليجرام مش بيشتغلوا من نفسهم من غير حساب خارجي**: مفيش طريقة تقنية تبعت واتساب لأي رقم تلقائيًا من غير حساب WhatsApp Business API معتمد (زي Twilio أو UltraMsg) — مش قصور في الكود، ده قيد من واتساب نفسها لمنع السبام. بنيت الكود جاهز (`app/Services/ApplicationNotifier.php`)، وبمجرد ما تجيب بيانات أي خدمة من دول وتحطها في `.env` (`WHATSAPP_API_URL`, `WHATSAPP_API_TOKEN`) هيشتغل تلقائي من غير أي تعديل تاني في الكود.
- **تيليجرام بيوصل للإدارة مش للعميل**: تيليجرام بوت مينفعش يبعت رسالة لأي حد إلا لو هو كلّم البوت الأول (قيد أمان من تيليجرام نفسها)، وإحنا مش بناخد "معرّف تيليجرام" العميل في الفورم. فالتنفيذ الحالي: بوت التيليجرام بيبعتلك **أنت** كإدارة تنبيه فوري بأي طلب جديد (ده مفيد عمليًا كـ "جرس" فوري)، والعميل بياخد تأكيده الرسمي عبر الإيميل. لو عايز تبعت للعميل نفسه على تيليجرام، محتاجين نضيف حقل "معرّف/يوزر تيليجرام" في فورم الطلب.
- **صورة يوري جاجارين**: زي ما شرحت في نقطة 5، حطيت صورة NASA حقيقية شغالة بدل ما أستنى، وسيبتلك التعليمات لاستبدالها بصورة جاجارين لما تجيبها.

---

## دخول لوحة التحكم
`/admin/login` → `admin@poyekhali.com` / `ChangeMe123!` (غيّرها زي ما اتشرح في التحديث السابق).

## حاجات ممكن تتضاف بعدين لو حبيت
- حقل "معرّف تيليجرام" في فورم الطلب عشان الإشعار يوصل للعميل مباشرة مش للإدارة بس.
- صفحة "طلباتي" للعميل بعد تسجيل الدخول (تربط حساب المستخدم بطلباته تلقائيًا).
- رفع صور الدول من نفس صفحة "ابدأ طلبك" (مثلاً بانر الدولة المختارة).

قولّي أي حاجة محتاجة تعديل أو فيها مشكلة وأنا أظبطها فورًا.
