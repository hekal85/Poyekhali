# تحديث v4 — رد على كل الملاحظات

## التركيب
```powershell
cd D:\work\Poyekhali
powershell -ExecutionPolicy Bypass -File "المسار\update-v4.ps1" -ZipPath "المسار\poyekhali-visa-v4-kit.zip"
```
السكريبت هيسألك سؤال واحد بس لو لقى عندك `HandleInertiaRequests.php` موجود بالفعل (تفاصيل تحت).

---

## رد على كل نقطة

**1) الكاروسيل باهت**: السبب كان طبقة تعتيم فوق الصور (`bg-ink` بشفافية 85-90%) — خفّفتها. وكمان استبدلت طريقة التبديل من حسابات CSS معقدة (كانت مضبوطة بالظبط على 4 صور بس) بكود JS بسيط (`setInterval`) بيشتغل صح مع أي عدد صور.
   - **مكان إضافة صورك العشرة**: حط الملفات في `public/images/hero/` (مثلاً `1.jpg` لغاية `10.jpg`، أو أي عدد وأي أسماء تحبها).
   - **مكان الكود اللي تعدّله**: `resources/js/pages/Home.vue`، دوّر على متغيّر `heroSlides` (فيه تعليق فوقه يوضح بالظبط) وبدّل القيم بمسارات صورك، مثلاً:
     ```js
     const heroSlides = ['/images/hero/1.jpg', '/images/hero/2.jpg', '/images/hero/3.jpg'];
     ```

**2) قائمة تحت اسم المستخدم**: لما تسجّل دخول، اسمك في الهيدر بقى زرار بيفتح قائمة فيها: **لوحة التحكم** (تظهر بس لو حسابك أدمن)، **حسابك**، **طلباتك**، **تسجيل الخروج**.

**3) جرس الإشعارات**: رصاصي لو مفيش تنبيهات، بيتحول أزرق وعليه رقم أحمر لما فيه إشعارات جديدة (تغيير حالة طلب، إلخ). بيفتح قائمة منسدلة فيها آخر 20 إشعار، مع رابط لكل واحد يودّيك لتفاصيل طلبك.

**4) لوحة تحكم العميل (`/dashboard`)**: فيها قسمين — **طلباتك** (بكل حالة طلب بلونها) مع زرار "طلب جديد" يوديك لصفحة `/apply`، و**الإشعارات** (بنفس محتوى الجرس، مع "تحديد الكل كمقروء"). سبت جزء "الرسائل بينك وبين الإدارة" زي ما اقترحت — مش موجود، ممكن نضيفه بعدين لو احتجته فعلاً.

**5+6) Modal نجاح الطلب**: بعد ما تقدّم طلب، بيظهرلك modal بعلامة صح خضرا، فيه رقم الطلب بخط كبير واضح، وتحته نص "احتفظ برقم الطلب ده لمتابعة وتتبع حالة طلبك" بالظبط زي ما طلبت، مع زرار نسخ الرقم وزرار يوديك مباشرة لصفحة التتبع.

**7+8) بحث وفلاتر في شاشة الطلبات (`/admin/applications`)**: مربع بحث برقم الطلب/جواز السفر/الاسم، بالإضافة لفلاتر منفصلة: الحالة، الدولة، نوع التأشيرة — وكلهم بيشتغلوا مع بعض في نفس الوقت (تقدر تفلتر "الملغاة" بس، أو "جارٍ الاستخراج في السعودية" مثلاً).

**9+10) إشعار المستخدم عند تغيير حالة الطلب**: لما تغيّر حالة أي طلب من لوحة التحكم:
   - بيتسجّل **إشعار داخل الموقع** (يظهر في الجرس ولوحة تحكم العميل) — بس لو الطلب كان اتعمل وهو مسجّل دخول (يعني له `user_id`).
   - بيتبعت **إيميل** تلقائي للعميل (لو دخل إيميله وقت التقديم).
   - **تيليجرام/واتساب**: نفس الآلية والقيود اللي شرحتها قبل كده (محتاجين بيانات حساب خارجي في `.env`) — دلوقتي بقى بيشتغل مع أي تحديث حالة، مش بس عند تقديم الطلب.

**11+12) الفوتر ديناميكي بالكامل**:
   - عمود "الدول المتاحة" بقى بيجيب من قاعدة البيانات (السعودية، الإمارات، قطر، الكويت — زي ما حددت بالظبط)، ولما تضغط على دولة بتوديك لصفحتها اللي فيها كل أنواع التأشيرات المتاحة + زر "قدم طلب".
   - عمود جديد "أنواع التأشيرات" برضه من قاعدة البيانات، ولما تضغط على نوع بيوديك لصفحة جديدة (`/visa-types/{key}`) بتوريك في كام دولة هو متاح، بكارت لكل دولة (بالعلم، الرسوم، مدة الإجراءات) وزرار "قدم طلب الآن" بجنب كل كارت.

**13) أكورديون أنواع التأشيرات في تعديل الدولة**: بدل ما تكون كل الأنواع مفتوحة فوق بعض، دلوقتي كل نوع عنوانه قابل للطي/الفتح، والعنوان بقى بالظبط شكل `نوع تأشيرة # "tourist"` (يعني الـ `key`) بدل رقم تسلسلي، زي ما طلبت.

**14) محاذاة بيانات التواصل في الفوتر**: المشكلة كانت إن `dir="ltr"` على الـ div كله كان بيغيّر محاذاة السطر نفسه (يخليه شمال) بدل ما يظبط بس ترتيب الأرقام/الحروف جواه. الحل الصح: شلت الـ `dir="ltr"` من الحاوية، وحطيته بس على كل قيمة لوحدها (الرقم/الإيميل/اليوزر) جوه `<span>`. كده السطر نفسه بيتماشى مع اتجاه الصفحة (يمين في العربي زي الفقرة اللي فوقه بالظبط، شمال في الإنجليزي)، لكن الأرقام والإيميل بيتقروا صح دايمًا.

**15) الإحصائيات حية من قاعدة البيانات**: قرّرت مبنيش جدول إحصائيات منفصل محتاج تحديث يدوي - بدل كده الأرقام بتتحسب مباشرة (live query) في كل مرة حد يفتح الصفحة الرئيسية:
   - **عدد الدول**: `Country::where('is_active', true)->count()`
   - **طلب اتنفذ**: عدد الطلبات اللي وصلت لحالة "التأشيرة جاهزة" فعليًا (مش مجرد كل الطلبات المُرسلة، عشان يكون مؤشر ثقة حقيقي)
   - **عملاء اتساعدوا**: عدد المستخدمين المسجّلين (مش أدمن)
   - **سنين الخبرة**: بتتحسب تلقائيًا من 1/1/2023 لتاريخ اليوم
   - **ليه مش جدول منفصل؟** لأن الـ live query أدق 100% ومفيش احتمال إنه "يفضل قديم" لو حد نسي يحدّثه يدويًا، وعدد الصفوف صغير جدًا فمفيش أي تأثير على الأداء. لو المشروع كبر جدًا لاحقًا وحبيت تسريع الصفحة الرئيسية، وقتها نضيف جدول cache بسيط.

---

## ملحوظة تقنية مهمة: HandleInertiaRequests.php
عشان الجرس وقائمة المستخدم والفوتر الديناميكي يشتغلوا، لازم Laravel "يشارك" بيانات معينة (اسم المستخدم، عدد الإشعارات، دول الفوتر) مع كل صفحة تلقائيًا. ده بيتم من ملف `app/Http/Middleware/HandleInertiaRequests.php`.

- **لو مشروعك اتعمل بستارتر كيت Laravel الرسمي** (اللي بيجيب Inertia+Vue جاهزين)، الملف ده غالبًا موجود عندك بالفعل من الأول. السكريبت هيلاحظ كده ويسألك تستبدله ولا تدمج يدويًا.
- لو اخترت الدمج اليدوي، افتح الملف عندك ودّي دالة `share()` وضيف المفاتيح: `footerCountries`، `footerVisaTypes`، `unreadNotificationsCount` (الكود الكامل موجود في نسخة الحزمة كمرجع).

## دخول لوحة التحكم
`/admin/login` → `admin@poyekhali.com` / `ChangeMe123!`

## حاجات ممكن تتضاف بعدين لو حبيت
- نظام رسائل مباشر بين العميل والإدارة داخل لوحة التحكم بتاعته (سبتها زي ما قلت إنها مش أولوية).
- تخصيص الدول الأربعة في الفوتر من لوحة التحكم بدل تعديل الكود (حاليًا في `HandleInertiaRequests.php`).
- Push notifications فعلية (متصفح) بدل الاعتماد على فتح الموقع لرؤية الجرس.

قولّي أي حاجة محتاجة تعديل وأنا أظبطها فورًا.
